need to define faces externally and generate data to pass to shader

issues-
 * multi-quad voxel face. 
	-need to build a structure such that a dynamic amount of quads is possible and addressable.
 * greedy meshing.
	- in conjunction with multi-quad faces, *some* faces need to be greedy meshed. This may be a component of a face with multiple quads.
	
 * greedy meshing consideration:
	- some faces must be listed as "stretchable", others simply added to the face data output.

CORE STRUCTURE:

	FaceList- will have all face data for a voxel ID
		-up pos, color, UV, custom, etc
		-North
		-east
		-south
		-west
		-down
		-greedy boolean
		-index to next face (or -1 if none)

	GreedyStructure- must be atomic, it is being written to
		6*128*128*128 face arrays, 
		
	process- 
		for each item in chunk data, check if applicable face. Search facetree with block ID, loop through facelist from that location and assign all non-stretch faces to quad list.
		
		once that's done, do greedy meshing step maybe? if applicable? idk I think greedy meshing might be too much. Who knows, I sure don't. 
		
		
	TO DO:
		COLOR and TEXTURE, AGAIN!!
		
		
		
		
		later goals:
			transp mesh (for water)
			greedy meshing (can skip for now, theoretically)
				- to work with baked AO, add 1000 to ID for first case, 2000 for 2nd case, so on,
				to prevent greedying of baked AO

			
		Texture Writer-
			import 64x64 images onto texture atlas
			
		Voxel editor-
			make new voxels!
			-select face (base default template not deleteable)
			-add quads to it
			-put texture onto each thing (select which faces get new UV scale or something, checkboxes?)
			-have list of all created voxels
			
			-pregenerate 256 colors to just hang on to, for reasons
